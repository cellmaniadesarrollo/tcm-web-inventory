import { RelativeDateZ0Pipe } from './relative-date-z0.pipe';

describe('RelativeDateZ0Pipe', () => {
  it('create an instance', () => {
    const pipe = new RelativeDateZ0Pipe();
    expect(pipe).toBeTruthy();
  });
});
